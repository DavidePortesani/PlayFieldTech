package it.unipv.ingsfw.playfieldtech.observer;

// Interfaccia che rappresenta un observer, che deve implementare il metodo update() per ricevere notifiche
public interface Observer {
    void update();
}
